<ul class="thumbnails">
    <% for @link in @links %>
        <li class="span4">
            <a href="<%= @link.url %>">
                <img src="<%= @link.photo.read %>" alt="">
            </a>
        </li>
    <% end %>
</ul>
