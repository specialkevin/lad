<ul class="thumbnails">
    <% for @link in @links %>
        <li class="span4">
            <a href="<%= @link.url %>">
                <img src="<% if @link.photo_id == NIL %>
                    <%= @link.url %>
                    <% else %>
                        <%= @link.photo.read %>
                    <% end %>" alt="">
            </a>
        </li>
    <% end %>
</ul>
